@model UsersModel
@{
    ViewData["Title"] = "Usuários";
}
<div class="text-center">
    <h3>Usuários</h3>
    <a id="botaoNovoBanco" type="button" class="btn btn-success float-end" asp-controller="Users" asp-action="Add">Novo Usuário</a>
    <br>
    <br>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>ID</th>
                <th>Nome</th>
                <th>Email</th>
                <th>Edição de registro</th>
                <th>Exclusão de registro</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var user in Model.GetUsers())
            {
                <tr>
                    <td>@user.Id.ToString("D5")</td>
                    <td>@user.Nome</td>
                    <td>@user.Email</td>
                    <td>
                        <div>
                            <a id="botaoEditarBanco" type="button" class="btn" asp-route-id="@user.Id" asp-controller="Users" asp-action="Update">Editar Usuário</a>
                        </div>
                    </td>
                    <td>
                        <div>
                            <a id="botaoExcluirBanco" type="button" class="btn" asp-route-id="@user.Id" asp-controller="Users" asp-action="Delete">Deletar Usuário</a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>